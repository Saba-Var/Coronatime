export type ArrowType = {
  clickOptions: {
    isClicked: boolean
    sort: boolean
    target: boolean
  }
}

export type ArrowUpProps = {
  clickOptions: {
    isClicked: boolean
    target: boolean
    sort: boolean
  }
}
